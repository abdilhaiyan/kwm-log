---
phase: 04-sortable-list-reordering
plan: 02
type: execute
wave: 1
gap_closure: true
gap_ids:
  - G-04-2
depends_on: []
name: "Arrow affordance — directional up/down glyphs in Settings reorder controls"
files_modified:
  - index.html
autonomous: true
human_verify_mode: end-of-phase
requirements:
  - SORT-01
  - SORT-02
  - SORT-03
  - SORT-04
  - SORT-05
  - SORT-06
user_setup: []
estimate:
  tokens: 16000
  raw_tokens: 8000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - LTR: Settings reorder controls clearly distinguish Up from Down with friendly icons — a directional up-arrow glyph renders above a directional down-arrow glyph in every (non-edit-mode) ListEditor row under Settings (index.html).
    - ATR: the icon swap is exactly one-for-one — `name="ChevronUp"` / `name="ChevronDown"` persist nowhere in index.html; `name="ArrowUp"` / `name="ArrowDown"` appear exactly once each (the Up and Down reorder buttons, index.html lines 117-118).
    - SCL: boundary conditions are byte-identical — up stays disabled when the row is first or its value is the literal `Other`; down stays disabled when the row is `Other`, is the last row, or the row immediately below is `Other` (live/legacy data may legitimately show `Other` not-last; the toggle must never let `Other` move).
    - B01: the icon-materialization loop is intact — `setTimeout(() => lucide.createIcons(), 100)` occurs in exactly 3 places (requests snapshot, options snapshot, settings-nav navigate): the new arrow glyphs materialize as `svg.lucide-arrow-up` / `svg.lucide-arrow-down` and no bare `<i data-lucide>` icon node remains in the rendered Settings DOM.
    - UGL: the neutral glass styling is untouched — enabled reorder buttons keep `bg-white/20 text-white/80 hover:bg-white/30 border border-white/30` (this exact tuple sits on both reorder buttons and on no other element), disabled keeps `bg-white/5 text-white/20 border-white/10 cursor-not-allowed`, and the legend keeps its single neutral `Reorder` dot entry.
    - TTL: hover tooltips are unchanged — the strings 'Move up', 'Move down', 'Already at top', 'Already at bottom', 'Cannot reorder Other' keep their exact existing text (desktop hover aid only; on staff phones, glyph direction and shape are the primary affordance).
  backstops:
    - LTR+B01: Sweep A and the DevTools expression both pass — arrow glyphs render in the live Settings DOM (`svg.lucide-arrow-up`/`-down` count >= 6 each, bare icon count 0) before the plan is considered done.
    - SCL: if any boundary regresses, Sweep B's byte assertions on the two disabled conditions, the tooltip ternaries, the onMoveUp/onMoveDown call counts, the createIcons count, and the legend entry fail — the executor halts and re-reads index.html lines 117-118.
  prohibitions:
    - No icon-color or icon-fill accents — Edit (blue), Delete (red), Cancel (orange), Save (emerald) already occupy the palette, and UAT requires a single neutral legend dot for Reorder.
    - No change to any code other than the icon prop on index.html lines 117-118 — and on those two lines, no change to onClick, disabled, title, or className.
    - No new lucide.createIcons() call sites; no lucide CDN pin change (lucide@1.44.0 stays).
  artifacts:
    - index.html
  key_links:
    - Settings tab -> ListEditor render -> per-row icon prop (index.html line 117 `<Icon name="ArrowUp" size={16} />`, line 118 `<Icon name="ArrowDown" size={16} />`) is the affordance surface G-04-2 names.
---

<objective>
**Goal (gap from 04-UAT.md):** "Settings reorder controls clearly distinguish Up from Down with friendly icons."

This plan closes G-04-2 — phase 04's only remaining open gap (severity: minor, triage: quick-fix in place). The Settings ListEditor currently renders `ChevronUp`/`ChevronDown` at `size={14}` on index.html lines 117-118 with identical neutral-glass styling, so staff cannot tell which control moves a row up and which moves it down — exactly what the UAT user reported ("hard to know which ones is up and which one is down") and asked to fix by redesigning the icon ("little up arrow and down arrow is okay to make it much friendly and easier").

Per UAT triage, this is a **redesign of the existing Reorder control**, not a new feature: one atomic two-token edit in index.html (the only source file in the repo), zero framework/tooling changes, zero data changes.

Impacted code: index.html **lines 117-118 only** — the two reorder `<button>` elements inside `ListEditor` (the row-actions column in Settings). Entire lines otherwise byte-identical.

Success indicators:
- The Settings reorder column shows a directional up arrow above a directional down arrow (16px glyphs, neutral glass) — readable as up/down at a glance.
- All move semantics, ordering boundaries, tooltips, styling tuples, and the legend entry are byte-identical to the shipped 04-01 state (LTR/ATR/SCL/B01/UGL/TTL truths).
- Staff phone (touch): no tooltip needed to read the control; both glyphs still held within the existing 32px button.
</objective>

<execution_context>
@C:/Users/KWM-ACCOUNT/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/KWM-ACCOUNT/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

# Gap source (do NOT modify — the orchestrator reconciles gap status)
@.planning/phases/04-sortable-list-reordering/04-UAT.md

# Conventions + change-site map + live-data anchors for phase 04
@.planning/phases/04-sortable-list-reordering/04-PATTERNS.md
@.planning/phases/04-sortable-list-reordering/04-RESEARCH.md

# Prior plan: verify-idiom (node -e regex sweeps) + Q1/Q4 amendments this plan preserves
@.planning/phases/04-sortable-list-reordering/04-01-PLAN.md
</context>

## Source Audit

| Source | Item | Coverage |
|--------|------|----------|
| UAT G-04-2 | Reorder control: hard to tell up from down; user asked for friendly up/down arrows | Task 1 (ArrowUp/ArrowDown at size 16, neutral glass kept) |
| ROADMAP REQ SORT-01..SORT-06 | Requirement IDs for phase 04 reorder work | Task 1 preserves every SORT behavior 04-01 shipped (see Sweep B) |
| RESEARCH Pitfall-1 | createIcons only runs from 3 snapshot sites; moving or duplicating it breaks icon rendering | No-change zone + Sweep B `ci == 3` + DevTools bare-icon check |
| RESEARCH live-data | Legacy row ordering (e.g. `Other` not last in one list) drives the last-index and below-is-`Other` clauses | SCL truth: boundary conditions byte-identical |
| CONTEXT | No CONTEXT.md decisions exist for phase 04 (discussion produced UAT + plan Q&As, now in 04-01) | n/a — no locked decisions to map |
| Deferred / out-of-scope | WR-02 "unpinned lucide@latest CDN URL" (Research warnings), Tailwind v4, React 19 | Not in scope for this gap; CDN pins untouched |

Exclusions (not gaps): WR-02 CDN pinning is a research warning on the existing `@latest` tag, not a UAT gap; the plan deliberately does NOT touch CDN URLs.

<tasks>

<task type="auto">
  <name>Task 1: Swap chevron reorder glyphs for directional ArrowUp/ArrowDown (size 16) on index.html lines 117-118</name>
  <files>index.html</files>
  <action>
    Make exactly two token edits in index.html — the icon prop inside the Up and Down reorder buttons of `ListEditor` (lines 117-118). Do not change anything else on either line.

    Line 117 — change the icon prop of the Up (move-up) button from:
    <!-- planner-discipline-allow: name="ChevronUp" -->
    `&lt;Icon name="ChevronUp" size={14} /&gt;`
    to:
    `&lt;Icon name="ArrowUp" size={16} /&gt;`

    Line 118 — change the icon prop of the Down (move-down) button from:
    <!-- planner-discipline-allow: name="ChevronDown" -->
    `&lt;Icon name="ChevronDown" size={14} /&gt;`
    to:
    `&lt;Icon name="ArrowDown" size={16} /&gt;`

    Preserve byte-for-byte on the SAME lines, untouched:
    - onClick: `onClick={() => onMoveUp(i)}` (117) / `onClick={() => onMoveDown(i)}` (118)
    - disabled conditions: up is `i === 0 || o === 'Other'`; down is `o === 'Other' || i >= items.length - 1 || items[i+1] === 'Other'` (the Q1-amended last-index clause — do NOT simplify; legacy data may hold `Other` not-last)
    - title ternaries: up `'Cannot reorder Other' : 'Already at top' : 'Move up'`, down `'Cannot reorder Other' : 'Already at bottom' : 'Move down'` — keep exact strings
    - className template: enabled `bg-white/20 text-white/80 hover:bg-white/30 border border-white/30`, disabled `bg-white/5 text-white/20 border-white/10 cursor-not-allowed`, container `p-2 rounded-lg transition-all border`

    Why: UAT-04 G-04-2 — the user cannot tell which Settings arrow moves a row up vs down. Directional arrow glyphs (pointing at the target position) at size 16 inside the existing 32px button make the control self-describing even on touch devices where tooltips never surface. Keeping the neutral glass styling avoids color collisions with the Edit/Delete/Cancel/Save accents and keeps the legend's single neutral Reorder dot valid (per UAT test 1's legend wording).

    Use the existing toolchain: react/Pencil icons are rendered via the `Icon` data-lucide mechanism, so the new glyph names materialize automatically — do NOT add or move any `lucide.createIcons()` calls; the 3 existing snapshot sites (requests, options, settings-nav) re-materialize on every render. Do NOT touch `index.html` anything except lines 117-118: not the `moveOption`/`appendItem`/`insertItem`/`util`/`removeItem` helpers, not `addOption`, not the three `ListEditor` call sites, not the legend block, not the CDN pins (lucide@1.44.0, React 18.3.1, ReactDOM 18.3.1, Babel 8.0.5, Tailwind Play CDN, Firebase compat 11.6.1).

    Commit after the edit.
  </action>
  <verify>
    <automated>
      # Sweep A — the fix itself. Run from repo root (PowerShell). Exit 1 on any assertion failure.
      node -e "const fs=require('fs');const s=fs.readFileSync('index.html','utf8');const cn=(re)=>(s.match(re)||[]).length;const a={au:cn(/name=.ArrowUp./g),ad:cn(/name=.ArrowDown./g),cu:cn(/name=.ChevronUp./g),cd:cn(/name=.ChevronDown./g),s16:cn(/size=\{16\}/g),up:cn(/'Move up'/g),down:cn(/'Move down'/g),enCls:cn(/bg-white\/20 text-white\/80 hover:bg-white\/30 border border-white\/30/g)};console.log(JSON.stringify(a));if(a.au!==1||a.ad!==1||a.cu!==0||a.cd!==0||a.s16!==2||a.up!==1||a.down!==1||a.enCls!==2)process.exit(1)"

      # Sweep B — no-change zones / boundary preservation. Exit 1 on any assertion failure.
      node -e "const fs=require('fs');const s=fs.readFileSync('index.html','utf8');const cn=(re)=>(s.match(re)||[]).length;const a={upDis:cn(/disabled=\{i === 0 \|\| o === 'Other'\}/g),downDis:cn(/disabled=\{o === 'Other' \|\| i >= items\.length - 1 \|\| items\[i\+1\] === 'Other'\}/g),top:cn(/'Already at top'/g),bottom:cn(/'Already at bottom'/g),otherT:cn(/'Cannot reorder Other'/g),upd:cn(/onMoveUp/g),dnd:cn(/onMoveDown/g),ci:cn(/setTimeout\(\(\) => lucide\.createIcons\(\), 100\)/g),legend:cn(/span>Reorder<\/span>/g)};console.log(JSON.stringify(a));if(a.upDis!==1||a.downDis!==1||a.top!==1||a.bottom!==1||a.otherT!==2||a.upd!==5||a.dnd!==5||a.ci!==3||a.legend!==1)process.exit(1)"
    </automated>
    <human-check>
      # DevTools — served app (python -m http.server 8080), http://localhost:8080
      1. Open the app in a browser, sign in anonymously (Staff view), open Settings (gear icon).
      2. Wait ~200ms for lucide.createIcons() to run, then evaluate in the console:
         (() => { const bare=document.querySelectorAll('i[data-lucide]').length; const up=document.querySelectorAll('svg.lucide-arrow-up').length; const down=document.querySelectorAll('svg.lucide-arrow-down').length; const ok=[bare===0,up>=6,down>=6].every(Boolean); console.log(JSON.stringify({bare:bare,up:up,down:down,ok:ok})); if(!ok){ throw new Error('icon materialization failed') } return {bare:bare,up:up,down:down}; })()
         Assert result: bare === 0, up >= 6, down >= 6 (3 lists x at least 2 rows each on live data).
      3. Visual check (phone-width viewport): each reorder column shows a directional up-arrow ABOVE a down-arrow; first row of each list still cannot move up (dimmed up arrow); the `Other` row shows both arrows dimmed; enabled arrows keep the neutral white-glass pill buttons.
      4. Interaction check: in Settings, move an item down and back up with the new arrows — order changes and persists exactly as before 04-02 (SORT-02..SORT-04 behaviors).
      Then prompt the user to re-confirm the gap item via `/gsd-verify-work 04` (resume: UAT tests 2/3/4 for G-04-2). Do NOT edit 04-UAT.md — the orchestrator reconciles gap status.
    </human-check>
  </verify>
  <done>index.html lines 117-118 render `ArrowUp size 16` (up) and `ArrowDown size 16` (down); zero `ChevronUp`/`ChevronDown` tokens remain in the file; both reorder buttons keep their exact disabled conditions, tooltip ternaries, and neutral glass classes; all three `lucide.createIcons()` sites intact; the Settings legend entry unchanged; arrows materialize in the live DOM with zero bare icon nodes.</done>
</task>

</tasks>

## No-Change Zones

- `moveOption` / `appendItem` / `insertItem` / `util` / `removeItem` projections and the LEGACY section (static list edits) — untouched.
- `addOption` handler — untouched.
- Three `ListEditor` call sites (index.html ~442-444) — untouched.
- Legend block (~445-452, entries "Reorder, Edit, Delete, Save, Cancel, ADD" with a single neutral dot for Reorder) — untouched.
- All CDN pins (lucide@1.44.0, React/ReactDOM 18.3.1, Babel 8.0.5, Tailwind Play CDN, Firebase compat 11.6.1) — untouched.

<threat_model>
## Threat Model (STRIDE)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02-01 | Tampering | index.html lines 117-118 (reorder buttons) | high | mitigate | Edit-scope creep: an executor swapping icons must not touch the disabled conditions, title ternaries, or className. Sweep B asserts both disabled conditions, all five tooltip strings, onMoveUp/onMoveDown counts (5 each), and the enabled-class tuple (2) byte-exactly; the plan's no-change zones forbid touching moveOption/addOption/call sites/legend. |
| T-04-02-02 | Tampering | lucide.createIcons() call sites (3) | medium | mitigate | Pitfall-1 regression: moving or duplicating a createIcons site leaves new arrow glyphs unmaterialized (blank squares) or double-rendered. Sweep B asserts `ci == 3`; the DevTools expression asserts `svg.lucide-arrow-up/-down >= 6` and bare `i[data-lucide] == 0`. The executor is explicitly told not to add or move any createIcons call. |
| T-04-02-03 | Spoofing / UX misdirection | Settings reorder controls (the gap itself) | medium | mitigate | The G-04-2 failure: identical chevron glyphs make up/down indistinguishable at a glance. Directional ArrowUp/ArrowDown at size 16 give an at-a-glance, color-blind-safe distinction; tooltips 'Move up'/'Move down' remain as hover aid. Deliberately NOT color-accented (would collide with Edit=blue/Delete=red/Cancel=orange/Save=emerald and break the single-neutral-dot legend). |
| T-04-02-04 | Tampering | index.html CDN <script> pins | low | accept | Pinned exact versions (React 18.3.1, Babel 8.0.5, Lucide 1.44.0, Firebase compat 11.6.1) are already static; this plan makes no CDN edits. WR-02 (pin lucide@latest exactly) is a research warning scoped out of this gap closure; pinned URLs remain a separate tracked concern. |
</threat_model>

<verification>
Overall phase checks for 04-02:
- Both `node -e` sweeps (A fix + B no-change zones) exit 0 — run from the repo root in PowerShell.
- DevTools expression on the served app (localhost:8080) returns `bare: 0` and `up >= 6`, `down >= 6` after opening Settings.
- Visual + interaction check on phone-width viewport (steps 3-4 of the human-check).
- Human gate note: the end-of-phase human verification (`/gsd-verify-work 04`, resuming UAT tests 2/3/4) is the authoritative confirmation that G-04-2 is resolved; do not mark the gap closed in 04-UAT.md yourself.
</verification>

<success_criteria>
- The reorder column in Settings reads as "up arrow / down arrow" at a glance on a 360px-wide viewport (G-04-2, LTR truth).
- `index.html` contains exactly zero `ChevronUp`/`ChevronDown` tokens and exactly one `ArrowUp` + one `ArrowDown` (ATR truth).
- Every move-boundary condition, tooltip string, styling tuple, helper, call site, and legend entry is byte-identical to the 04-01 shipped state (SCL/UGL/TTL truths); Sweep B passes.
- All three `lucide.createIcons()` sites intact and the new arrows materialize in the live DOM (B01 truth); DevTools expression passes.
- Single-file, no-build, CDN-loaded React app constraint fully preserved (compatibility requirement).
</success_criteria>

<output>
Create `.planning/phases/04-sortable-list-reordering/04-02-SUMMARY.md` when done (per the standard summary template).
</output>