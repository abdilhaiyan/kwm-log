---
phase: 02-config-driven-passcode
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [.planning/PROJECT.md, .planning/research/FEATURES.md, .planning/research/ARCHITECTURE.md]
autonomous: true
requirements: [PASS-01, PASS-02, PASS-03]

estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - PROJECT.md describes the manager passcode as the `DEFAULT_PASSCODE` HTML constant (edit to rotate), not as Firestore-backed or changeable via the app.
    - FEATURES.md describes constant-based validation with no Firestore passcode read and no rotation field; its validation table no longer claims a change-passcode flow.
    - ARCHITECTURE.md no longer references Firestore passcode fetch/update services or a passcode state mirror; `DEFAULT_PASSCODE` remains as the sole passcode source.
  artifacts:
    - .planning/PROJECT.md — Validated list, Context → Firebase bullet, Constraints → Security, Key Decisions (edited)
    - .planning/research/FEATURES.md — Manager authentication gate row rewritten; change-passcode validation row replaced with constant-rotation row (edited)
    - .planning/research/ARCHITECTURE.md — passcode-flow references removed/updated (edited)
  key_links:
    - All three docs now agree with the new code reality in index.html (no Firestore passcode path) — docs stay truthful for future phases (D-03).
---

<objective>
Refresh the three planning documents named in D-03 so they describe the constant-based passcode: the passcode is the `DEFAULT_PASSCODE` constant in index.html Block 2, rotation means editing that constant, and Firestore is no longer involved in passcode validation or rotation (PASS-01, PASS-02, PASS-03).

Purpose: Keep planning docs truthful for future phases — stale Firestore-passcode descriptions would mislead the next planner/executor. Doc-only work; zero runtime impact.

Output:
- .planning/PROJECT.md: validated-requirement row, Firebase context bullet, Security constraint, Key Decisions row revised.
- .planning/research/FEATURES.md: Manager authentication gate row rewritten; "Change passcode" validation row replaced with a constant-rotation row.
- .planning/research/ARCHITECTURE.md: passcode-flow references removed/updated at the listed locations; ES-module hypothetical section left otherwise intact.
</objective>

<execution_context>
@C:/Users/KWM-ACCOUNT/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/KWM-ACCOUNT/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/research/FEATURES.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/02-config-driven-passcode/2-CONTEXT.md
@.planning/phases/02-config-driven-passcode/02-PATTERNS.md

Line numbers are as of 2026-09-14; locate edit targets by content as well as line number. Do NOT edit .planning/research/{STACK,SUMMARY,PITFALLS}.md or .planning/STATE.md — D-03 names exactly the three files above.
</context>

<tasks>

<task type="auto">
  <name>Update PROJECT.md — passcode is an HTML constant, not Firestore-backed (D-03)</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md — Validated list (lines 13-34), Context (lines 51-58), Constraints (lines 60-66), Key Decisions (lines 68-78)
  </read_first>
  <action>
    Per D-03, make four surgical edits in .planning/PROJECT.md, preserving the file's markdown style (tab-separated bullets/tables, existing ✓/- markers, no new emojis):

    1. Validated list, passcode row (line 30): revise so the parenthetical describes a constant, not a Firestore-stored changeable value — e.g. "Passcode-protected manager actions (HTML constant DEFAULT_PASSCODE, edit to rotate) — existing". Leave the adjacent Enter-key row (line 33) untouched.
    2. Context → Firebase bullet (line 54): remove the sentence describing the app config doc and its changeable passcode. Replace with: the manager passcode is the `DEFAULT_PASSCODE` constant in index.html Block 2 — rotating it means editing that constant and reloading. Optionally note the old app config doc in Firestore still exists as harmless dead data that the app never reads (D-02: leave it untouched, no cleanup).
    3. Constraints → Security bullet (line 65): replace the "Firestore-backed, changeable" phrasing with the constant model — manager actions gated by the passcode constant in HTML; rotate by editing DEFAULT_PASSCODE. Keep "No user accounts."
    4. Key Decisions table, passcode row (line 75): change the row so it documents the supersession — Decision "Passcode as HTML constant (DEFAULT_PASSCODE)", Rationale "rotate by editing the constant; no Firestore dependency", Outcome "✓ Good (Phase 2)".
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/PROJECT.md','utf8');if(s.includes('Firestore-backed, changeable')){console.error('FAIL stale constraint');process.exit(1)}if(s.includes('changeable, stored in Firestore')){console.error('FAIL stale requirement');process.exit(1)}if(!s.includes('DEFAULT_PASSCODE')){console.error('FAIL constant missing');process.exit(1)}if(!s.includes('edit to rotate')){console.error('FAIL rotation wording missing');process.exit(1)}console.log('OK PROJECT.md')"</automated>
    <human-check>Read the four edited spots — wording is natural, no leftover claims of a Firestore-backed changeable passcode.</human-check>
  </verify>
  <acceptance_criteria>
    - Lines 30, 54, 65, 75 revised as specified; no other lines change.
    - The phrases "Firestore-backed, changeable" and "changeable, stored in Firestore" no longer appear; `DEFAULT_PASSCODE` and "edit to rotate" wording appear.
  </acceptance_criteria>
  <done>PROJECT.md describes the constant-based passcode in all four spots; automated check exits 0; committed.</done>
</task>

<task type="auto">
  <name>Update FEATURES.md — constant-based validation, no rotation flow (D-03)</name>
  <files>.planning/research/FEATURES.md</files>
  <read_first>
    - .planning/research/FEATURES.md — Table Stakes rows 23 and 40; Validation Checklist rows 165-166
  </read_first>
  <action>
    Per D-03, edit .planning/research/FEATURES.md:

    1. Line 23 — "Manager authentication gate" row: rewrite the What-Must-Be-Preserved and Notes cells so validation is against the HTML constant `DEFAULT_PASSCODE = "1234"` (Block 2). Remove all wording about a Firestore app config read, a fallback value, and an optional new-passcode field. Keep the preserved-flow wording: modal → validate against the constant → set `isManagerAuthenticated` → switch to manager view. Complexity stays MEDIUM (or lower if you judge it LOW — complexity is advisory; behavior description is what matters).
    2. Line 40 — "Enter key on passcode" row: leave untouched (behavior preserved).
    3. Line 165 — "Passcode gate" validation row: leave untouched (expected behavior identical).
    4. Line 166 — "Change passcode" validation row: the control no longer exists, so replace the row with one documenting the rotation model — Scenario "Passcode rotation", Steps "Edit the DEFAULT_PASSCODE constant in index.html and reload", Expected "New value is required to unlock the manager view". (See 02-PATTERNS.md line 177.)
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/research/FEATURES.md','utf8');if(s.includes('Change passcode')){console.error('FAIL stale row');process.exit(1)}if(s.includes('validated against Firestore')){console.error('FAIL stale gate description');process.exit(1)}if(!s.includes('DEFAULT_PASSCODE')){console.error('FAIL constant missing');process.exit(1)}if(!s.includes('Rotation')){console.error('FAIL rotation row missing');process.exit(1)}console.log('OK FEATURES.md')"</automated>
    <human-check>Read rows 23, 40, 165, 166 — the gate row reads correctly with the constant model; Enter-key and wrong-passcode rows untouched.</human-check>
  </verify>
  <acceptance_criteria>
    - Row 23 rewritten to constant-based validation; rows 40 and 165 byte-identical; row 166 replaced with the rotation row.
    - No "Change passcode" scenario and no "validated against Firestore" phrasing remain; `DEFAULT_PASSCODE` and a "Rotation" scenario appear.
  </acceptance_criteria>
  <done>FEATURES.md describes constant-based validation and the rotation model; automated check exits 0; committed.</done>
</task>

<task type="auto">
  <name>Update ARCHITECTURE.md — remove the Firestore passcode-flow references (D-03)</name>
  <files>.planning/research/ARCHITECTURE.md</files>
  <read_first>
    - .planning/research/ARCHITECTURE.md — component tables (lines 42-71), recommended architecture (lines 82-129 and 190-240), data flow (lines 259-327), build order (lines 399-435)
    - .planning/phases/02-config-driven-passcode/02-PATTERNS.md — "ARCHITECTURE.md (D-03 doc update)" section (the authoritative per-line change list)
  </read_first>
  <action>
    Per D-03, update only the passcode-flow references in .planning/research/ARCHITECTURE.md, following the per-line list in 02-PATTERNS.md. Do NOT rewrite the "Phase 2: ES Modules + Import Maps" hypothetical section wholesale — edit only the passcode references inside it.

    1. Line 44 — Config row: keep `DEFAULT_PASSCODE` in the globals list; annotate it as the sole passcode source, editable to rotate.
    2. Line 45 — Firestore reads/writes row: drop the passcode-fetch mention from the Purpose cell; keep the onSnapshot listener, submitRequest, updateStatus, markAsReturned.
    3. Line 68 — PasscodeModal component row: remove the rotation state props and the Firestore-provenance note; state that validation compares against the `DEFAULT_PASSCODE` constant and there is no rotation control.
    4. Lines 109-110 — future Firebase Service block: delete the two passcode service entries (fetch + update).
    5. Line 211 — future app.js imports: keep the PasscodeModal import (component still exists). No rotation-related phrasing there — confirm only.
    6. Line 221 — future passcode-modal.js file comment: replace "passcode rotation" phrasing (e.g. "Admin gate (constant passcode)").
    7. Lines 231-232 — future firebase.js exports: delete the two passcode service exports.
    8. Line 269 — decomposed data-flow effects: drop "passcode fetch".
    9. Line 284 — future Firebase Service list: drop the fetch/update passcode entries.
    10. Line 327 — state ownership table: delete the row for the passcode state mirror entirely (that state no longer exists).
    11. Line 378 — internal boundaries table: keep `DEFAULT_PASSCODE` in the shared-globals cell; adjust the note so it does not imply a Firebase service reads the passcode (the constant is read directly by the App Shell).
    12. Lines 414-416 — Phase 1 build order, Block 3 extract list: remove the fetch/update passcode items; keep subscribeRequests, submitRequest, updateStatus.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/research/ARCHITECTURE.md','utf8');const bad=['fetchPasscode','updatePasscode','currentPasscode','newPasscode'];const h=bad.filter(x=>s.includes(x));if(h.length){console.error('FAIL: '+h.join(','));process.exit(1)}if(!s.includes('DEFAULT_PASSCODE')){console.error('FAIL constant missing');process.exit(1)}if(s.includes('passcode rotation')){console.error('FAIL rotation phrasing remains');process.exit(1)}console.log('OK ARCHITECTURE.md')"</automated>
    <human-check>Spot-read the twelve locations — the ES-module hypothetical section still reads coherently with only passcode references removed.</human-check>
  </verify>
  <acceptance_criteria>
    - No occurrence of fetchPasscode, updatePasscode, currentPasscode, or newPasscode remains; `DEFAULT_PASSCODE` still present; no "passcode rotation" phrasing remains in the component/service comments.
    - Only passcode-flow references changed — the ES-module hypothetical section is otherwise intact.
  </acceptance_criteria>
  <done>ARCHITECTURE.md has no Firestore passcode-service or passcode-state references; all other content intact; automated check exits 0; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning docs → future phases | Stale Firestore-passcode descriptions in these docs would be read as truth by the next planner/executor and cause wrong assumptions or re-introduction of the removed path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Integrity | .planning/PROJECT.md, FEATURES.md, ARCHITECTURE.md | low | mitigate | Acceptance-criteria negative greps (node one-liners) + read-through human-check keep the docs from re-asserting the removed Firestore passcode path (D-03). |
| T-02-SC | Tampering | npm/pip/cargo installs | high | accept | No package installations this phase — doc edits only; nothing to vet. |
</threat_model>

<verification>
Run from the repo root. All checks are single-command `node -e` one-liners (Phase 1 pattern; fall back to temp-dir scripts if PowerShell quoting bites). Commit discipline: one commit per document (Phase 1 locked decision D-02) — three commits total for this plan, or one combined docs commit if the repo convention prefers (match 02-01's commit style).

The three docs are text-only; no browser verification required. Human review reads the edited sections during the end-of-phase verify phase (config `human_verify_mode: end-of-phase`).
</verification>

<success_criteria>
- PROJECT.md, FEATURES.md, ARCHITECTURE.md all describe the constant-based passcode and contain no Firestore passcode-flow claims (D-03).
- PASS-01/PASS-02/PASS-03 are reflected in doc wording: constant as source, no Firestore passcode logic, modal flow preserved.
- No out-of-scope doc edited ({STACK,SUMMARY,PITFALLS}.md and STATE.md untouched).
</success_criteria>

<output>
Create `.planning/phases/02-config-driven-passcode/02-02-SUMMARY.md` when done.
</output>

<!-- planner-discipline-allow: fetchPasscode -->
<!-- planner-discipline-allow: updatePasscode -->
<!-- planner-discipline-allow: currentPasscode -->
<!-- planner-discipline-allow: newPasscode -->