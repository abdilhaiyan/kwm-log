---
phase: 01-multi-block-decomposition
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - index.html
autonomous: true
requirements:
  - REF-01
  - REF-02
  - REF-03
  - REF-04
  - REF-05
must_haves:
  truths:
    - "App loads without console errors after each block extraction"
    - "All 14 useState hooks remain in the App component (behavior contract)"
    - "All 5 useEffect hooks remain in the App component with their cleanup functions (behavior contract)"
    - "All event handlers remain in the App component (behavior contract)"
    - "Components receive state + callbacks via props — prop drilling, not Context (behavior contract)"
    - "Every extraction is a pure move — code copied, not edited (behavior contract)"
    - "Field names, status strings, and payload construction are byte-identical (locked decision D-04)"
    - "data-passcode-unlock DOM query preserved exactly as-is (locked decision D-04)"
    - "lucide.createIcons() 100ms timeout preserved exactly as-is (locked decision D-04)"
    - "firebaseConfig, app, auth, db, signIn() exist in Block 3 (Firebase Service)"
    - "Icon, Notification, PasscodeModal exist in Block 4 (Shared UI)"
    - "Manager passcode gate works (Enter key submits) after extraction (verification checklist)"
    - "Passcode input starts empty with placeholder; Unlock button disabled until input (UI-SPEC covered: passcode-modal empty)"
    - "Wrong passcode shows error notification toast; modal stays open (UI-SPEC covered: passcode-modal error)"
    - "Toast is fixed at bottom, full-width on mobile, max-w on desktop; auto-dismisses after 4000ms via setTimeout — decomposition must preserve the timer (UI-SPEC covered: notification-toast overflow)"
    - "No loading spinner exists — decomposition must NOT add one (UI-SPEC covered: request-list loading)"
  artifacts:
    - index.html
  key_links:
    - "Block 3 (Firebase Service) initializes firebase.app before Block 4 (Shared UI) references db for passcode save"
    - "Block 4 (Shared UI) PasscodeModal uses the Firebase db ref from Block 3"
    - "Script order: Constants → Utilities → Firebase Service → Shared UI → original inline block"
---

<objective>
Extract the Firebase Service and Shared UI layers from the monolithic inline `text/babel` block into blocks 3 and 4 of the seven-block decomposition.

Purpose: Move the data layer (Firebase init + anonymous auth) and the reusable UI primitives (Icon, Notification, PasscodeModal) out of the monolith. These are the first blocks with side effects (Firebase init) and the first React components extracted — proving components defined in an earlier block are usable by the App in a later block via shared global scope.

Output: `index.html` with 4 new script blocks (Constants, Utilities, Firebase Service, Shared UI) + the original inline block minus the extracted code. Two commits (one per block, per locked decision D-02).
</objective>

<execution_context>
@C:/Users/KWM-ACCOUNT/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/KWM-ACCOUNT/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-multi-block-decomposition/1-CONTEXT.md
@.planning/phases/01-multi-block-decomposition/01-UI-SPEC.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@index.html
</context>

<tasks>

<task type="tracer">
  <name>Extract Block 3: Firebase Service</name>
  <files>index.html</files>
  <action>
    Per locked decision D-01 (inline blocks) and D-02 (one commit per block):

    1. Read `index.html`. In the original inline block, locate the Firebase initialization code: `firebaseConfig`, `firebase.initializeApp(...)` (or equivalent), `firebase.auth()` → `auth`, `firebase.firestore()` → `db`, and the `signIn()` function (per CONTEXT.md build order row 3). Copy them exactly as written — byte-identical, including comments.
    2. Insert a NEW `<script type="text/babel">` block AFTER the Utilities block (Block 2) and BEFORE the original inline block, containing ONLY the Firebase service code.
    3. Remove the extracted Firebase code from the original inline block. Do NOT touch anything else.
    4. Verify script order: Constants → Utilities → Firebase Service → original inline block.
    5. Commit with message: `refactor(01-02): extract firebase service block` — stage ONLY `index.html`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const blocks=(h.match(/<script type=\"text\/babel\">/g)||[]).length;const f=h.match(/firebaseConfig|firebase\.initializeApp|firebase\.auth\(\)|firebase\.firestore\(\)|function signIn/g)||[];if(blocks!==4)throw new Error('expected 4 text/babel blocks, got '+blocks);if(f.length<5)throw new Error('firebase symbols missing: '+f.length);console.log('OK: 4 blocks, '+f.length+' firebase refs')"</automated>
  </verify>
  <done>
    index.html has exactly 4 text/babel blocks in order Constants → Utilities → Firebase Service → original; firebaseConfig, app init, auth, db, and signIn() exist in Block 3; the original inline block no longer contains them; app reloads without console errors; Firestore data still loads (request list populates); icons still render. Committed as `refactor(01-02): extract firebase service block`.
  </done>
</task>

<task type="auto">
  <name>Extract Block 4: Shared UI</name>
  <files>index.html</files>
  <action>
    Per locked decision D-01 (inline blocks) and D-02 (one commit per block):

    1. Read `index.html`. In the original inline block, locate the shared UI component definitions: `Icon`, `Notification`, and `PasscodeModal` (per CONTEXT.md build order row 4). Copy them exactly as written — byte-identical, including comments and any props they reference.
    2. Insert a NEW `<script type="text/babel">` block AFTER the Firebase Service block (Block 3) and BEFORE the original inline block, containing ONLY the shared UI components.
    3. Remove the extracted components from the original inline block. Do NOT touch anything else.
    4. Verify script order: Constants → Utilities → Firebase Service → Shared UI → original inline block.
    5. Commit with message: `refactor(01-02): extract shared ui block` — stage ONLY `index.html`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const blocks=(h.match(/<script type=\"text\/babel\">/g)||[]).length;const s=h.match(/function Icon|function Notification|function PasscodeModal/g)||[];if(blocks!==5)throw new Error('expected 5 text/babel blocks, got '+blocks);if(s.length<3)throw new Error('shared ui missing: '+s.length);console.log('OK: 5 blocks, '+s.length+' shared ui components')"</automated>
  </verify>
  <done>
    index.html has exactly 5 text/babel blocks in order Constants → Utilities → Firebase Service → Shared UI → original; Icon, Notification, and PasscodeModal exist in Block 4; the original inline block no longer contains them; app reloads without console errors; manager passcode gate works (Enter key submits); wrong passcode shows error toast; icons render in all views. Committed as `refactor(01-02): extract shared ui block`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Firestore | Existing Firebase auth + Firestore access; unchanged by this phase (pure code movement) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | index.html script blocks | medium | mitigate | Script order enforced by task verify (Firebase Service before Shared UI — Shared UI's PasscodeModal needs the db ref); each extraction verified by reload + spot check before commit (locked decision D-03) |
| T-01-05 | Spoofing | Client-side passcode gate | low | accept | Existing documented limitation (client-side passcode, Firestore-backed); out of scope per CONTEXT.md scope boundaries — no change in this phase |
| T-01-06 | Information Disclosure | Firebase config in client bundle | low | accept | Existing design (client-side Firebase config is public by nature); unchanged by this phase |
| T-01-07 | Denial of Service | Firebase init ordering | medium | mitigate | Block 3 (Firebase init) placed before Block 4 (Shared UI) in script order; verify checks block count + symbol presence before commit |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase — zero new dependencies (REF-05) |
</threat_model>

<verification>
- After each task: reload `index.html` via `python -m http.server` (or equivalent) and confirm the app renders identically with no console errors (locked decision D-03).
- Spot check after each extraction: request list populates from Firestore, manager passcode gate (Enter key), wrong-passcode toast, icon rendering.
- Final: `node` verify scripts above pass (block count + symbol presence).
</verification>

<success_criteria>
- index.html contains exactly 5 text/babel blocks in dependency order (Constants → Utilities → Firebase Service → Shared UI → original inline).
- firebaseConfig/app/auth/db/signIn() exist in Block 3; Icon/Notification/PasscodeModal exist in Block 4; all absent from the original inline block.
- Zero user-visible change: app renders identically, no console errors, Firestore data loads, passcode gate works.
- Two commits created: `refactor(01-02): extract firebase service block`, `refactor(01-02): extract shared ui block`.
- REF-01, REF-02, REF-03, REF-04, REF-05 all advanced by this plan (decomposition continued, data + shared UI layers extracted, behaviors preserved, single-file deployment intact).
</success_criteria>

<output>
Create `.planning/phases/01-multi-block-decomposition/01-02-SUMMARY.md` when done
</output>