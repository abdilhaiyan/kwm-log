---
phase: 01-multi-block-decomposition
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - index.html
autonomous: true
requirements:
  - REF-01
  - REF-02
  - REF-03
  - REF-04
  - REF-05
must_haves:
  truths:
    - "App loads without console errors after each block extraction"
    - "All 14 useState hooks remain in the App component (behavior contract)"
    - "All 5 useEffect hooks remain in the App component with their cleanup functions (behavior contract)"
    - "All event handlers remain in the App component (behavior contract)"
    - "Components receive state + callbacks via props — prop drilling, not Context (behavior contract)"
    - "Every extraction is a pure move — code copied, not edited (behavior contract)"
    - "Field names, status strings, and payload construction are byte-identical (locked decision D-04)"
    - "data-passcode-unlock DOM query preserved exactly as-is (locked decision D-04)"
    - "lucide.createIcons() 100ms timeout preserved exactly as-is (locked decision D-04)"
    - "EquipmentForm and ManagerView exist in Block 5 (View Components)"
    - "App component exists in Block 6 (App Shell) with all 14 useState + 5 useEffect + all event handlers"
    - "ReactDOM.createRoot(...).render(<App />) exists in Block 7 (Render)"
    - "Dark mode toggle works after extraction (verification checklist)"
    - "Equipment form submits (DJI, Car, Other variants) after extraction (verification checklist)"
    - "Manager passcode gate works (Enter key submits) after extraction (verification checklist)"
    - "Request cards render with correct status colors after extraction (verification checklist)"
    - "Approve/Reject flow works after extraction (verification checklist)"
    - "'ITEM PASSED' button works after extraction (verification checklist)"
    - "Return flow works after extraction (verification checklist)"
    - "Detail modal shows all fields after extraction (verification checklist)"
    - "Filter + search works after extraction (verification checklist)"
    - "CSV export works after extraction (verification checklist)"
    - "Clipboard copy works on phone (HTTP, non-secure) after extraction (verification checklist)"
    - "Icons render in all views after extraction (verification checklist)"
    - "Unseen badge count works after extraction (verification checklist)"
    - "Zero requests renders 'No borrowing requests yet' empty state with Inbox icon; zero filtered matches renders 'No requests match your filters' (UI-SPEC covered: request-list empty)"
    - "Populated list renders request cards with borrower name, equipment, status badge, unseen badge, per-status action buttons (UI-SPEC covered: request-list populated)"
    - "Cards with missing optional fields (no plat number, no fuel level) render without those rows — fields conditionally rendered (UI-SPEC covered: request-list partial)"
    - "Request list scrolls vertically (max-h + overflow-y-auto); overdue indicator truncates (UI-SPEC covered: request-list overflow)"
    - "One card per request; no plural copy logic; unseen badge shows exact count 1–99, '99+' at 100+, hidden at 0 (UI-SPEC covered: request-list zero-one-many)"
    - "Pills render with count 0 until snapshot loads; pills with count 0 are hidden (UI-SPEC covered: status-pills loading)"
    - "Pill counts derive from the same snapshot as the list — no separate error state; decomposition must preserve (UI-SPEC covered: status-pills error)"
    - "Pill row wraps on narrow screens; counts are small integers (UI-SPEC covered: status-pills overflow)"
    - "Modal opens with data already loaded from the request object — no loading state (UI-SPEC covered: detail-modal loading)"
    - "No in-modal error state — action failures show 'Error updating' toast (UI-SPEC covered: detail-modal error)"
    - "Purpose text wraps in modal (max-w-[60%]); long equipment names wrap (UI-SPEC covered: detail-modal long-text)"
    - "CONFIRM RETURN shows no loading state — Firestore update is async but button does not disable (UI-SPEC covered: return-modal loading)"
    - "Update failure shows 'Error updating' toast; modal stays open (UI-SPEC covered: return-modal error)"
    - "Nav renders immediately with static content (logo, tabs, toggle) — no loading state (UI-SPEC covered: top-nav loading)"
    - "Empty state is centered icon + short text — no overflow concern (UI-SPEC covered: empty-state overflow)"
    - "Borrower name and equipment label truncate with `truncate` class; purpose wraps in detail modal — visual check on narrow phone viewport (UI-SPEC backstop: request-list long-text)"
    - "Modal scrolls internally (max-h-[85vh] overflow-y-auto) for long content — visual check with a request containing long purpose text (UI-SPEC backstop: detail-modal overflow)"
  artifacts:
    - index.html
  key_links:
    - "Block 5 (View Components) references Icon/Notification/PasscodeModal from Block 4, constants from Block 1, utilities from Block 2"
    - "Block 6 (App Shell) composes EquipmentForm + ManagerView from Block 5 and passes state/callbacks via props"
    - "Block 7 (Render) mounts the App from Block 6 via ReactDOM.createRoot"
    - "Script order: Constants → Utilities → Firebase Service → Shared UI → View Components → App Shell → Render"
---

<objective>
Extract the View Components, App Shell, and Render layers from the monolithic inline `text/babel` block into blocks 5, 6, and 7 — completing the seven-block decomposition.

Purpose: This is the final plan. It moves the presentational components (EquipmentForm, ManagerView), the App shell (all state, effects, handlers), and the render call out of the monolith. When complete, the original inline block is empty and removed, and index.html contains exactly 7 ordered script blocks with zero user-visible change.

Output: `index.html` with 7 script blocks (Constants, Utilities, Firebase Service, Shared UI, View Components, App Shell, Render) and no remaining inline block. Three commits (one per block, per locked decision D-02).
</objective>

<execution_context>
@C:/Users/KWM-ACCOUNT/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/KWM-ACCOUNT/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-multi-block-decomposition/1-CONTEXT.md
@.planning/phases/01-multi-block-decomposition/01-UI-SPEC.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@index.html
</context>

<tasks>

<task type="tracer">
  <name>Extract Block 5: View Components</name>
  <files>index.html</files>
  <action>
    Per locked decision D-01 (inline blocks) and D-02 (one commit per block):

    1. Read `index.html`. In the original inline block, locate the view component definitions: `EquipmentForm` and `ManagerView` (per CONTEXT.md build order row 5). ManagerView includes inline request cards and modals. Copy them exactly as written — byte-identical, including comments, props, and any inline sub-components (request cards, modals) they contain.
    2. Insert a NEW `<script type="text/babel">` block AFTER the Shared UI block (Block 4) and BEFORE the original inline block, containing ONLY the view components.
    3. Remove the extracted components from the original inline block. Do NOT touch anything else.
    4. Verify script order: Constants → Utilities → Firebase Service → Shared UI → View Components → original inline block.
    5. Commit with message: `refactor(01-03): extract view components block` — stage ONLY `index.html`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const blocks=(h.match(/<script type=\"text\/babel\">/g)||[]).length;const v=h.match(/function EquipmentForm|function ManagerView/g)||[];if(blocks!==6)throw new Error('expected 6 text/babel blocks, got '+blocks);if(v.length<2)throw new Error('view components missing: '+v.length);console.log('OK: 6 blocks, '+v.length+' view components')"</automated>
  </verify>
  <done>
    index.html has exactly 6 text/babel blocks in order Constants → Utilities → Firebase Service → Shared UI → View Components → original; EquipmentForm and ManagerView exist in Block 5; the original inline block no longer contains them; app reloads without console errors; form submits (DJI, Car, Other variants); request cards render with correct status colors; approve/reject/pass/return flows work; icons render. Committed as `refactor(01-03): extract view components block`.
  </done>
</task>

<task type="auto">
  <name>Extract Block 6: App Shell</name>
  <files>index.html</files>
  <action>
    Per locked decision D-01 (inline blocks) and D-02 (one commit per block):

    1. Read `index.html`. In the original inline block, locate the `App` component definition (per CONTEXT.md build order row 6). It contains ALL 14 useState hooks, ALL 5 useEffect hooks (with their cleanup functions), and ALL event handlers. Copy it exactly as written — byte-identical, including comments.
    2. Insert a NEW `<script type="text/babel">` block AFTER the View Components block (Block 5) and BEFORE the original inline block, containing ONLY the App component.
    3. Remove the App component from the original inline block. Do NOT touch anything else.
    4. Verify script order: Constants → Utilities → Firebase Service → Shared UI → View Components → App Shell → original inline block.
    5. Commit with message: `refactor(01-03): extract app shell block` — stage ONLY `index.html`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const blocks=(h.match(/<script type=\"text\/babel\">/g)||[]).length;const a=h.match(/function App|const App/g)||[];const s=h.match(/useState/g)||[];const e=h.match(/useEffect/g)||[];if(blocks!==7)throw new Error('expected 7 text/babel blocks, got '+blocks);if(a.length<1)throw new Error('App component missing');if(s.length<14)throw new Error('expected >=14 useState, got '+s.length);if(e.length<5)throw new Error('expected >=5 useEffect, got '+e.length);console.log('OK: 7 blocks, App present, '+s.length+' useState, '+e.length+' useEffect')"</automated>
  </verify>
  <done>
    index.html has exactly 7 text/babel blocks in order Constants → Utilities → Firebase Service → Shared UI → View Components → App Shell → original; App exists in Block 6 with all 14 useState + 5 useEffect (with cleanups) + all event handlers; the original inline block no longer contains it; app reloads without console errors; all interactions work (form, passcode, approve/reject, pass, return, filter, export, copy, dark mode, unseen badge). Committed as `refactor(01-03): extract app shell block`.
  </done>
</task>

<task type="auto">
  <name>Extract Block 7: Render and Remove Empty Inline Block</name>
  <files>index.html</files>
  <action>
    Per locked decision D-01 (inline blocks) and D-02 (one commit per block):

    1. Read `index.html`. In the original inline block, locate the render call: `ReactDOM.createRoot(...).render(<App />)` (per CONTEXT.md build order row 7). Copy it exactly as written — byte-identical.
    2. Insert a NEW `<script type="text/babel">` block AFTER the App Shell block (Block 6), containing ONLY the render call.
    3. Remove the render call from the original inline block. The original inline block should now be EMPTY — remove the empty `<script type="text/babel">` tag entirely.
    4. Verify final script order: Constants → Utilities → Firebase Service → Shared UI → View Components → App Shell → Render. Exactly 7 blocks, no empty blocks.
    5. Commit with message: `refactor(01-03): extract render block` — stage ONLY `index.html`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const blocks=(h.match(/<script type=\"text\/babel\">/g)||[]).length;const r=h.match(/ReactDOM\.createRoot|\.render\(<App/g)||[];const empty=h.match(/<script type=\"text\/babel\">\s*<\/script>/g)||[];if(blocks!==7)throw new Error('expected exactly 7 text/babel blocks, got '+blocks);if(r.length<1)throw new Error('render call missing');if(empty.length>0)throw new Error('empty script blocks found');console.log('OK: exactly 7 blocks, render present, no empty blocks')"</automated>
  </verify>
  <done>
    index.html has exactly 7 text/babel blocks in order Constants → Utilities → Firebase Service → Shared UI → View Components → App Shell → Render; render call exists in Block 7; no empty script blocks remain; app reloads without console errors; full verification checklist passes (all 15 items from CONTEXT.md); icons render in all views. Committed as `refactor(01-03): extract render block`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Firestore | Existing Firebase auth + Firestore access; unchanged by this phase (pure code movement) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Tampering | index.html script blocks | medium | mitigate | Script order enforced by task verify (View Components before App Shell before Render); each extraction verified by reload + spot check before commit (locked decision D-03) |
| T-01-09 | Spoofing | Client-side passcode gate | low | accept | Existing documented limitation (client-side passcode, Firestore-backed); out of scope per CONTEXT.md scope boundaries — no change in this phase |
| T-01-10 | Denial of Service | Babel compile failure on final assembly | medium | mitigate | Pure-move rule (byte-identical copy) prevents syntax drift; final verify checks exactly 7 blocks, render present, no empty blocks |
| T-01-11 | Elevation of Privilege | App component state/effect misplacement | medium | mitigate | Behavior contract enforced: all 14 useState + 5 useEffect + handlers stay in App (Block 6); verify counts useState/useEffect occurrences |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase — zero new dependencies (REF-05) |
</threat_model>

<verification>
- After each task: reload `index.html` via `python -m http.server` (or equivalent) and confirm the app renders identically with no console errors (locked decision D-03).
- Spot check after each extraction: form submit (DJI/Car/Other), passcode gate, approve/reject, ITEM PASSED, return flow, detail modal, filter + search, CSV export, clipboard copy, dark mode, unseen badge, icon rendering.
- Final: `node` verify scripts above pass (exactly 7 blocks, render present, no empty blocks).
- Post-phase: run the full 15-item verification checklist from CONTEXT.md.
</verification>

<success_criteria>
- index.html contains exactly 7 text/babel blocks in dependency order (Constants → Utilities → Firebase Service → Shared UI → View Components → App Shell → Render).
- EquipmentForm + ManagerView in Block 5; App (14 useState + 5 useEffect + handlers) in Block 6; render call in Block 7; no empty blocks.
- Zero user-visible change: full 15-item verification checklist from CONTEXT.md passes.
- Three commits created: `refactor(01-03): extract view components block`, `refactor(01-03): extract app shell block`, `refactor(01-03): extract render block`.
- REF-01, REF-02, REF-03, REF-04, REF-05 all satisfied by this plan (decomposition complete, utilities + constants extracted, all 17 behaviors preserved, single-file deployment intact).
</success_criteria>

<output>
Create `.planning/phases/01-multi-block-decomposition/01-03-SUMMARY.md` when done
</output>