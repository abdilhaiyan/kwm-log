---
phase: 01-multi-block-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - REF-01
  - REF-02
  - REF-03
  - REF-04
  - REF-05
must_haves:
  truths:
    - "App loads without console errors after each block extraction"
    - "All 14 useState hooks remain in the App component (behavior contract)"
    - "All 5 useEffect hooks remain in the App component with their cleanup functions (behavior contract)"
    - "All event handlers remain in the App component (behavior contract)"
    - "Components receive state + callbacks via props — prop drilling, not Context (behavior contract)"
    - "Every extraction is a pure move — code copied, not edited (behavior contract)"
    - "Field names, status strings, and payload construction are byte-identical (locked decision D-04)"
    - "data-passcode-unlock DOM query preserved exactly as-is (locked decision D-04)"
    - "lucide.createIcons() 100ms timeout preserved exactly as-is (locked decision D-04)"
    - "EQUIPMENT_OPTIONS, CONDITION_OPTIONS, CAMERA_MODELS, darkModeListenerAdded exist in Block 1 (Constants)"
    - "copyDetails(), formatDate(), truncate(), getReturnRequirement() exist in Block 2 (Utilities)"
    - "Dark mode toggle works after extraction (verification checklist)"
    - "Equipment form submits (DJI, Car, Other variants) after extraction (verification checklist)"
    - "Icons render in all views after extraction (verification checklist)"
    - "Unfilled form shows all fields empty with placeholders; no pre-filled data (UI-SPEC covered: equipment-form empty)"
    - "Submit button shows 'Processing...' while the Firestore add is in-flight (UI-SPEC covered: equipment-form loading)"
    - "Submit failure shows 'Error submitting' notification toast; form values preserved (UI-SPEC covered: equipment-form error)"
    - "Conditional fields per equipment type: Car → plat number select + Borrow Time + Fuel Level; Other → custom equipment input; DJI → neither; Email hidden for Car (UI-SPEC covered: equipment-form partial)"
    - "Purpose textarea and equipment name input accept long text; full-width inputs, no truncation (UI-SPEC covered: equipment-form long-text)"
    - "No loading spinner exists — decomposition must NOT add one (UI-SPEC covered: request-list loading)"
    - "List load failure has no explicit error UI (onSnapshot errors silent) — decomposition must preserve (UI-SPEC covered: request-list error)"
    - "Toast is fixed at bottom, full-width on mobile, max-w on desktop; auto-dismisses after 4000ms via setTimeout — decomposition must preserve the timer (UI-SPEC covered: notification-toast overflow)"
  artifacts:
    - index.html
  key_links:
    - "Block 1 (Constants) loads before Block 2 (Utilities) — script order in index.html"
    - "Block 2 (Utilities) references Block 1 constants via shared global scope"
    - "All 7 blocks remain inside index.html as separate <script type=\"text/babel\"> tags (locked decision D-01)"
---

<objective>
Extract the Constants and Utilities layers from the monolithic inline `text/babel` block into the first two of seven ordered `<script type="text/babel">` blocks, proving the multi-block decomposition mechanism end-to-end.

Purpose: This is the tracer plan — it proves that (a) multiple `text/babel` blocks in one `index.html` compile and execute in document order via Babel standalone, (b) shared global scope works across blocks without import/export, and (c) the app renders identically after the first extraction. Success here de-risks the remaining 5 extractions.

Output: `index.html` with 2 new script blocks (Constants, Utilities) + the original inline block minus the extracted code. Two commits (one per block, per locked decision D-02).
</objective>

<execution_context>
@C:/Users/KWM-ACCOUNT/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/KWM-ACCOUNT/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-multi-block-decomposition/1-CONTEXT.md
@.planning/phases/01-multi-block-decomposition/01-UI-SPEC.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@index.html
</context>

<tasks>

<task type="tracer">
  <name>Extract Block 1: Constants</name>
  <files>index.html</files>
  <action>
    Per locked decision D-01 (inline blocks) and D-02 (one commit per block):

    1. Read `index.html` in full. Locate the single inline `<script type="text/babel">` block.
    2. Identify the constants declarations inside it: `EQUIPMENT_OPTIONS`, `CONDITION_OPTIONS`, `CAMERA_MODELS`, and `darkModeListenerAdded` (per CONTEXT.md build order row 1). These may be declared with `const`, `let`, or `var` — copy them exactly as written, including any comments.
    3. Insert a NEW `<script type="text/babel">` block BEFORE the existing inline block, containing ONLY the extracted constants. The new block must be a pure move: byte-identical code, no edits, no renames, no reformatting.
    4. Remove the extracted constants from the original inline block. Do NOT touch anything else in the original block.
    5. Verify the script order: Constants block first, then the original (now smaller) inline block.
    6. Commit with message: `refactor(01-01): extract constants block` — stage ONLY `index.html`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const blocks=(h.match(/<script type=\"text\/babel\">/g)||[]).length;const c=h.match(/EQUIPMENT_OPTIONS|CONDITION_OPTIONS|CAMERA_MODELS|darkModeListenerAdded/g)||[];if(blocks!==2)throw new Error('expected 2 text/babel blocks, got '+blocks);if(c.length<4)throw new Error('constants missing: '+c.length);console.log('OK: 2 blocks, '+c.length+' constant refs')"</automated>
  </verify>
  <done>
    index.html has exactly 2 text/babel blocks; all 4 constants (EQUIPMENT_OPTIONS, CONDITION_OPTIONS, CAMERA_MODELS, darkModeListenerAdded) exist in the new Block 1; the original inline block no longer contains them; app reloads without console errors; dark mode toggle still works; icons still render. Committed as `refactor(01-01): extract constants block`.
  </done>
</task>

<task type="auto">
  <name>Extract Block 2: Utilities</name>
  <files>index.html</files>
  <action>
    Per locked decision D-01 (inline blocks) and D-02 (one commit per block):

    1. Read `index.html`. In the original inline block, locate the utility function declarations: `copyDetails()`, `formatDate()`, `truncate()`, and `getReturnRequirement()` (per CONTEXT.md build order row 2). Copy them exactly as written — byte-identical, including comments and any helper variables they close over.
    2. Insert a NEW `<script type="text/babel">` block AFTER the Constants block (Block 1) and BEFORE the original inline block, containing ONLY the extracted utilities.
    3. Remove the extracted utilities from the original inline block. Do NOT touch anything else.
    4. Verify script order: Constants → Utilities → original inline block.
    5. Commit with message: `refactor(01-01): extract utilities block` — stage ONLY `index.html`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const blocks=(h.match(/<script type=\"text\/babel\">/g)||[]).length;const u=h.match(/function copyDetails|function formatDate|function truncate|function getReturnRequirement/g)||[];if(blocks!==3)throw new Error('expected 3 text/babel blocks, got '+blocks);if(u.length<4)throw new Error('utilities missing: '+u.length);console.log('OK: 3 blocks, '+u.length+' utility fns')"</automated>
  </verify>
  <done>
    index.html has exactly 3 text/babel blocks in order Constants → Utilities → original; all 4 utility functions (copyDetails, formatDate, truncate, getReturnRequirement) exist in Block 2; the original inline block no longer contains them; app reloads without console errors; copy-details interaction still works; icons still render. Committed as `refactor(01-01): extract utilities block`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Firestore | Existing Firebase auth + Firestore access; unchanged by this phase (pure code movement) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | index.html script blocks | medium | mitigate | Script order enforced by task verify (Constants → Utilities → original); each extraction verified by reload + spot check before commit (locked decision D-03) |
| T-01-02 | Spoofing | Client-side passcode gate | low | accept | Existing documented limitation (client-side passcode, Firestore-backed); out of scope per CONTEXT.md scope boundaries — no change in this phase |
| T-01-03 | Denial of Service | Babel standalone compile failure | medium | mitigate | Pure-move rule (byte-identical copy) prevents syntax drift; verify checks block count + symbol presence before commit |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase — zero new dependencies (REF-05) |
</threat_model>

<verification>
- After each task: reload `index.html` via `python -m http.server` (or equivalent) and confirm the app renders identically with no console errors (locked decision D-03).
- Spot check after each extraction: dark mode toggle, one form interaction, icon rendering.
- Final: `node` verify scripts above pass (block count + symbol presence).
</verification>

<success_criteria>
- index.html contains exactly 3 text/babel blocks in dependency order (Constants → Utilities → original inline).
- All 4 constants and all 4 utility functions exist in their new blocks and are absent from the original inline block.
- Zero user-visible change: app renders identically, no console errors.
- Two commits created: `refactor(01-01): extract constants block`, `refactor(01-01): extract utilities block`.
- REF-01, REF-02, REF-03, REF-04, REF-05 all advanced by this plan (decomposition started, utilities + constants extracted, behaviors preserved, single-file deployment intact).
</success_criteria>

<output>
Create `.planning/phases/01-multi-block-decomposition/01-01-SUMMARY.md` when done
</output>