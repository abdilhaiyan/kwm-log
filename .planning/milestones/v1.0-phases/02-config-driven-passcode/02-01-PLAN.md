---
phase: 02-config-driven-passcode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.html]
autonomous: true
requirements: [PASS-01, PASS-02, PASS-03]

estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - Manager unlock validates the typed passcode against the HTML constant `DEFAULT_PASSCODE` ("1234") and, on match, switches to the manager view (PASS-01, PASS-03).
    - No passcode read or write touches Firestore — the app config doc fetch effect and the rotation write block are gone (PASS-02).
    - Wrong-passcode behavior is byte-identical to before: input clears, no toast.
    - The rotation control (second input + "Passcode updated" toast) is gone; `handleNotify` remains for other toasts.
    - The file still has exactly 7 `text/babel` blocks; modal chrome and the other four effects are byte-identical.
  artifacts:
    - index.html — Block 2 rotation comment; Block 6 passcode state/effect/handler edits
  key_links:
    - Unlock handler compares against the Block 2 constant: `passcodeAttempt === DEFAULT_PASSCODE`
    - Enter key → `document.querySelector('[data-passcode-unlock]').click()` → same handler (preserved byte-identical)
    - Only remaining Firestore read is the borrowing_requests `onSnapshot` listener (currently line 150), byte-identical
---

<objective>
Manager unlock becomes config-driven: validation reads the `DEFAULT_PASSCODE` constant defined in Block 2 (Config) instead of a Firestore-mirrored React state, and the entire Firestore passcode path (fetch effect + rotation write) is removed. The passcode modal UI stays visually and behaviorally identical except that the rotation input is gone. The Firestore app config doc is left untouched as dead data (D-02 — no programmatic deletion, no cleanup task).

Purpose: Eliminate a database read at startup, remove the only code that could mutate a passcode field, and make passcode rotation an HTML edit (D-01 comment). This satisfies PASS-01 (constant as the passcode source), PASS-02 (no Firestore passcode logic), PASS-03 (modal UI preserved).

Output:
- index.html edited: removed — `newPasscode` state, `currentPasscode` state, the `app_config` fetch `useEffect`, the "New passcode" input, the Firestore rotation write block, and the "Passcode updated" `handleNotify` call.
- index.html edited: validation comparison now `passcodeAttempt === DEFAULT_PASSCODE`; rotation comment added above the constant (D-01).
- Preserved byte-identical: `passcodeAttempt`/`showPasscodeModal` state, password input + Enter handler, `data-passcode-unlock`, wrong-passcode clear branch, Cancel button, modal chrome, the other four effects (incl. `lucide.createIcons()` 100ms timeout in the `onSnapshot` effect), `handleNotify` definition.
</objective>

<execution_context>
@C:/Users/KWM-ACCOUNT/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/KWM-ACCOUNT/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-config-driven-passcode/2-CONTEXT.md
@.planning/phases/02-config-driven-passcode/02-PATTERNS.md
@index.html

Line-number caveat: line numbers below are as of 2026-09-14. Edits shift later lines — locate code by content, re-check numbers after each edit.
</context>

<tasks>

<task type="tracer">
  <name>End-to-end "constant-based passcode" — validate against DEFAULT_PASSCODE; delete the Firestore passcode path and the rotation control</name>
  <files>index.html</files>
  <read_first>
    - index.html — Block 2 (Config, lines 48-59) and Block 6 (App Shell, lines 89-160; passcode modal, lines 420-448)
    - .planning/phases/02-config-driven-passcode/02-PATTERNS.md — Edit B, Edit C, Edit D sections (pre-change code + exact post-edit targets)
    - .planning/phases/02-config-driven-passcode/2-CONTEXT.md — decisions D-01/D-02 and the agent's discretion
    - .planning/phases/01-multi-block-decomposition/01-01-PLAN.md — Phase 1 verification pattern (node one-liners, lines 101-111) and locked decision D-04: preserve `data-passcode-unlock` + the `lucide.createIcons()` 100ms timeout as-is
  </read_first>
  <precondition>index.html currently has 7 `text/babel` blocks and contains the passcode state rows at lines 115-116, the app config fetch effect at lines 153-158, and the rotation input at line 427. If any of these is missing, stop and report — the file drifted from the Phase 1 handoff.</precondition>
  <reversibility rating="reversible">All changes are deletions plus a comparison swap inside one local file; git history is the undo path.</reversibility>
  <action>
    Perform three coordinated edits in index.html, each a pure surgical removal per 02-PATTERNS.md Edit B / Edit C / Edit D. No other lines change.

    Edit B — state removal (currently lines 115-116): delete the two lines declaring the rotation state and the Firestore mirror state. The input-attempt state and the modal-visible state (currently lines 97-98) stay byte-identical; all other useState hooks untouched. After this, no React state holds the passcode — validation reads the Block 2 constant directly.

    Edit C — effect removal (currently lines 153-158): delete the entire effect that fetches the app config doc from Firestore — the `if (!db) return;` guard, the `.collection(...).doc('app_config').get()` chain with its `.then`/`.catch`, and the empty dependency array. Keep the other four effects byte-identical (dark-mode persistence, anonymous-auth listener, borrowing_requests `onSnapshot` — which contains the `setTimeout(() => lucide.createIcons(), 100)` fix — and seen-requests persistence; currently lines 148-151). The `onSnapshot` listener becomes the ONLY Firestore read in the file.

    Edit D — validator and modal (currently lines 420-448): in the passcode modal, delete the second input (the rotation text field showing the "New passcode (optional)" placeholder, currently line 427) so the modal contains exactly one input — the password input with its class string and Enter-key handler, which stays byte-identical. In the Unlock button handler (currently lines 428-444): change the success condition to compare `passcodeAttempt` against the Block 2 constant (i.e. `passcodeAttempt === DEFAULT_PASSCODE`); keep the four success-branch statements in their current order — set manager-authenticated true, close the modal, switch the view to manager, mark requests seen; delete the entire conditional Firestore write block and its dependent state updates including the "Passcode updated" notification call (currently lines 434-440). Keep the wrong-passcode branch byte-identical: `setPasscodeAttempt("")` with no notification. Do NOT touch: the button's class string, the `data-passcode-unlock` attribute, the Cancel button, the modal container classes, the "Admin Access" title, or the overlay. Keep the `handleNotify` definition (currently ~line 160) — it still serves other toasts.

    Preserve Phase 1 locked decision D-04 exactly: the Enter-key `document.querySelector('[data-passcode-unlock]').click()` pattern stays as-is.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('index.html','utf8');const bad=['app_config','currentPasscode','newPasscode'];const h=bad.filter(x=>s.includes(x));if(h.length){console.error('FAIL: '+h.join(','));process.exit(1)}console.log('OK removed identifiers absent')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('index.html','utf8');if(!s.includes('passcodeAttempt === DEFAULT_PASSCODE')){console.error('FAIL constant compare');process.exit(1)}if(!s.includes('data-passcode-unlock')){console.error('FAIL unlock attr');process.exit(1)}if(!s.includes('setPasscodeAttempt(')){console.error('FAIL wrong-code reset');process.exit(1)}if(s.includes('Passcode updated')){console.error('FAIL stale toast');process.exit(1)}if(s.includes('New passcode (optional)')){console.error('FAIL rotation input remains');process.exit(1)}console.log('OK handler shape')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('index.html','utf8');const n=(s.match(/text\/babel/g)||[]).length;if(n!==7){console.error('FAIL blocks '+n);process.exit(1)}console.log('OK blocks '+n)"</automated>
    <human-check>End-of-phase browser UAT (config `human_verify_mode: end-of-phase`; Babel standalone cannot run in the preview env — Phase 1 finding). Serve with `python -m http.server 8080` and open http://10.0.6.12:8080: correct passcode 1234 unlocks the manager view; wrong passcode clears the input with no toast; the modal shows exactly ONE password field; Unlock button and Enter key both work; request list, dark-mode toggle, form submit render identically; no console errors.</human-check>
  </verify>
  <acceptance_criteria>
    - index.html contains no occurrence of the removed identifiers (app_config, currentPasscode, newPasscode) and no occurrence of the removed UI strings ("New passcode (optional)", "Passcode updated") — all four `node -e` checks above exit 0.
    - The only passcode validation is `passcodeAttempt === DEFAULT_PASSCODE` (PASS-01, PASS-03).
    - No Firestore passcode read or write remains (PASS-02); the borrowing_requests `onSnapshot` effect, anonymous-auth effect, dark-mode effect and seen-requests effect are byte-identical; the 100ms `createIcons` timeout is untouched.
    - Exactly 7 `text/babel` blocks remain; password input, Enter handler, `data-passcode-unlock`, wrong-code branch, Cancel button, and modal chrome are byte-identical.
  </acceptance_criteria>
  <done>Manager unlock validates against the Block 2 constant; Firestore passcode read/write and the rotation control are removed; all automated checks pass; committed (one commit for this logical change, per Phase 1 decision D-02).</done>
</task>

<task type="auto">
  <name>Annotate DEFAULT_PASSCODE with a rotation comment (D-01)</name>
  <files>index.html</files>
  <read_first>
    - index.html — Block 2 (Config, lines 48-59)
    - .planning/phases/02-config-driven-passcode/02-PATTERNS.md — Edit A section
  </read_first>
  <action>
    Per D-01: insert a single comment line `// Manager passcode — edit to rotate` immediately above the `const DEFAULT_PASSCODE = "1234";` line in Block 2 (Config). Nothing else in Block 2 changes — `firebaseConfig` and the `appId` line stay byte-identical. Zero runtime impact.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('index.html','utf8');const i=s.indexOf('edit to rotate');const j=s.indexOf('const DEFAULT_PASSCODE');if(i<0||i>j||j-i>100){console.error('FAIL');process.exit(1)}console.log('OK comment precedes constant')"</automated>
  </verify>
  <acceptance_criteria>
    - The comment appears on its own line directly above `const DEFAULT_PASSCODE = "1234";` in Block 2.
    - The constant value remains exactly `"1234"`.
    - Zero other changes in Block 2; blocks still total 7.
  </acceptance_criteria>
  <done>The rotation comment is present above the constant in index.html Block 2; the automated check exits 0; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → index.html code | The passcode constant ships in page source; any LAN client can read it. This is a UI gate for an internal tool, NOT a data-access boundary — Firestore rules are unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Spoofing | Unlock handler (`passcodeAttempt === DEFAULT_PASSCODE`) | medium | accept | Client-side constant gate is the locked design (ROADMAP goal + PASS-01); the value was already readable as the Firestore fallback before this phase; no data-access boundary is crossed either way (anonymous auth only, rules unchanged). |
| T-02-02 | Tampering | Removed rotation write (app config doc `.set`) | low | mitigate | The Firestore write path is deleted outright — no code path can mutate a passcode field anymore. |
| T-02-03 | Information Disclosure | Block 2 constant `"1234"` in source | low | accept | Internal LAN tool; the value was already exposed as the fallback and via the Firestore doc before; the D-01 rotation comment makes the operator-visible rotation model explicit. |
| T-02-04 | Elevation of Privilege | Manager view gate | medium | accept | Anyone reading the source can unlock — unchanged in substance from the pre-change design (single passcode, no accounts per PROJECT.md); this phase introduces no new privilege boundary. |
| T-02-05 | Denial of Service | Firestore request surface | low | accept | Removing the app config fetch + write reduces Firestore operations; no new unauthenticated path added. |
| T-02-SC | Tampering | npm/pip/cargo installs | high | accept | No package installations this phase (single-file HTML, no build step) — nothing to vet. |
</threat_model>

<verification>
Run from the repo root. All checks are single-command `node -e` one-liners (the established Phase 1 pattern); if PowerShell quoting ever bites, fall back to writing the same script to the temp dir (`C:\Users\KWM-AC~1\AppData\Local\Temp\opencode`) and running `node <script>`. Re-run the block-integrity check (7 `text/babel` blocks) after every edit.

Commit discipline (Phase 1 locked decision D-02): one commit per logical change — Task 1 (functional edits B+C+D) and Task 2 (D-01 comment) are separate commits.

End-of-phase human verification happens in the verify phase (config `human_verify_mode: end-of-phase`): serve over HTTP and run the UAT list from Task 1's human-check.
</verification>

<success_criteria>
- Manager unlock accepts "1234" and switches to the manager view; wrong input clears the field silently (PASS-01, PASS-03).
- Zero Firestore passcode references remain in index.html; `onSnapshot` is the only Firestore read (PASS-02).
- Modal has exactly one input; Unlock + Enter both work; `data-passcode-unlock` and the 100ms `createIcons` timeout preserved (D-04).
- Rotation annotation present above the constant (D-01); Firestore app config doc untouched (D-02).
- 7 `text/babel` blocks intact; app otherwise renders and behaves identically.
</success_criteria>

<output>
Create `.planning/phases/02-config-driven-passcode/02-01-SUMMARY.md` when done.
</output>

<!-- planner-discipline-allow: app_config -->
<!-- planner-discipline-allow: currentPasscode -->
<!-- planner-discipline-allow: newPasscode -->