---
phase: 01-multi-block-decomposition
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - index.html
autonomous: true
requirements:
  - REF-01
  - REF-02
  - REF-03
  - REF-04
  - REF-05
must_haves:
  truths:
    - "App loads without console errors after each block extraction"
    - "All 14 useState hooks remain in the App component (behavior contract)"
    - "All 5 useEffect hooks remain in the App component with their cleanup functions (behavior contract)"
    - "All event handlers remain in the App component (behavior contract)"
    - "Components receive state + callbacks via props — prop drilling, not Context (behavior contract)"
    - "Every extraction is a pure move — code copied, not edited (behavior contract)"
    - "Field names, status strings, and payload construction are byte-identical (locked decision D-04)"
    - "data-passcode-unlock DOM query preserved exactly as-is (locked decision D-04)"
    - "lucide.createIcons() 100ms timeout preserved exactly as-is (locked decision D-04)"
    - "app, auth, db exist in Block 3 (Firebase Init)"
    - "isOverdue, getDaysInfo, getRequestDuration, getEquipmentLabel, getEquipmentIcon exist in Block 4 (Utilities)"
    - "Manager passcode gate works (Enter key submits) after extraction (verification checklist)"
    - "Passcode input starts empty with placeholder; Unlock button disabled until input (UI-SPEC covered: passcode-modal empty)"
    - "Wrong passcode shows error notification toast; modal stays open (UI-SPEC covered: passcode-modal error)"
    - "Toast is fixed at bottom, full-width on mobile, max-w on desktop; auto-dismisses after 4000ms via setTimeout — decomposition must preserve the timer (UI-SPEC covered: notification-toast overflow)"
    - "No loading spinner exists — decomposition must NOT add one (UI-SPEC covered: request-list loading)"
  artifacts:
    - index.html
  key_links:
    - "Block 3 (Firebase Init) initializes app/auth/db before Block 4 (Utilities) — script order in index.html"
    - "Block 4 (Utilities) pure helpers are referenced by App (Block 6) via shared global scope"
    - "Script order: Constants → Config → Firebase Init → Utilities → original inline block"
---

<objective>
Extract the Firebase Init and Utilities layers from the monolithic inline `text/babel` block into blocks 3 and 4 of the seven-block decomposition.

Purpose: Move the data layer (Firebase init) and the pure helper functions out of the monolith. Block 3 is the first block with side effects (Firebase init). Block 4 is the first extraction that moves code OUT of the App component body — proving that functions defined in an earlier block are usable by the App in a later block via shared global scope.

Output: `index.html` with 4 new script blocks (Constants, Config, Firebase Init, Utilities) + the original inline block minus the extracted code. Two commits (one per block, per locked decision D-02).
</objective>

<execution_context>
@C:/Users/KWM-ACCOUNT/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/KWM-ACCOUNT/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-multi-block-decomposition/1-CONTEXT.md
@.planning/phases/01-multi-block-decomposition/01-UI-SPEC.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@index.html
</context>

<tasks>

<task type="tracer">
  <name>Extract Block 3: Firebase Init</name>
  <files>index.html</files>
  <action>
    Per locked decision D-01 (inline blocks) and D-02 (one commit per block):

    1. Read `index.html`. In the original inline block, locate the Firebase initialization code: `let app, auth, db;` and the `try { app = firebase.initializeApp(firebaseConfig); auth = firebase.auth(); db = firebase.firestore(); } catch (err) { console.error("Firebase init error:", err); }` block (per CONTEXT.md build order row 3). Copy them exactly as written — byte-identical, including comments.
    2. Insert a NEW `<script type="text/babel">` block AFTER the Config block (Block 2) and BEFORE the original inline block, containing ONLY the Firebase init code.
    3. Remove the extracted Firebase init code from the original inline block. Do NOT touch anything else.
    4. Verify script order: Constants → Config → Firebase Init → original inline block.
    5. Commit with message: `refactor(01-02): extract firebase init block` — stage ONLY `index.html`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const blocks=(h.match(/<script type=\"text\/babel\">/g)||[]).length;const f=h.match(/firebase\.initializeApp|firebase\.auth\(\)|firebase\.firestore\(\)/g)||[];if(blocks!==4)throw new Error('expected 4 text/babel blocks, got '+blocks);if(f.length<3)throw new Error('firebase init symbols missing: '+f.length);console.log('OK: 4 blocks, '+f.length+' firebase init refs')"</automated>
  </verify>
  <done>
    index.html has exactly 4 text/babel blocks in order Constants → Config → Firebase Init → original; app, auth, and db init exist in Block 3; the original inline block no longer contains them; app reloads without console errors; Firestore data still loads (request list populates); icons still render. Committed as `refactor(01-02): extract firebase init block`.
  </done>
</task>

<task type="auto">
  <name>Extract Block 4: Utilities</name>
  <files>index.html</files>
  <action>
    Per locked decision D-01 (inline blocks) and D-02 (one commit per block):

    1. Read `index.html`. Inside the `App` component in the original inline block, locate the pure helper function declarations: `isOverdue`, `getDaysInfo`, `getRequestDuration`, `getEquipmentLabel`, and `getEquipmentIcon` (per CONTEXT.md build order row 4). These are pure functions — they depend only on their arguments and `Date`, NOT on App state. Copy them exactly as written — byte-identical, including comments.
    2. Insert a NEW `<script type="text/babel">` block AFTER the Firebase Init block (Block 3) and BEFORE the original inline block, containing ONLY the extracted utilities.
    3. Remove the extracted utilities from the App component body. Do NOT touch anything else.
    4. Verify script order: Constants → Config → Firebase Init → Utilities → original inline block.
    5. Commit with message: `refactor(01-02): extract utilities block` — stage ONLY `index.html`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const blocks=(h.match(/<script type=\"text\/babel\">/g)||[]).length;const u=h.match(/isOverdue|getDaysInfo|getRequestDuration|getEquipmentLabel|getEquipmentIcon/g)||[];if(blocks!==5)throw new Error('expected 5 text/babel blocks, got '+blocks);if(u.length<5)throw new Error('utilities missing: '+u.length);console.log('OK: 5 blocks, '+u.length+' utility refs')"</automated>
  </verify>
  <done>
    index.html has exactly 5 text/babel blocks in order Constants → Config → Firebase Init → Utilities → original; isOverdue, getDaysInfo, getRequestDuration, getEquipmentLabel, and getEquipmentIcon exist in Block 4; the App component no longer contains them; app reloads without console errors; request cards render with correct status colors; copy-details interaction still works; icons still render. Committed as `refactor(01-02): extract utilities block`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Firestore | Existing Firebase auth + Firestore access; unchanged by this phase (pure code movement) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | index.html script blocks | medium | mitigate | Script order enforced by task verify (Firebase Init before Utilities); each extraction verified by reload + spot check before commit (locked decision D-03) |
| T-01-05 | Spoofing | Client-side passcode gate | low | accept | Existing documented limitation (client-side passcode, Firestore-backed); out of scope per CONTEXT.md scope boundaries — no change in this phase |
| T-01-06 | Information Disclosure | Firebase config in client bundle | low | accept | Existing design (client-side Firebase config is public by nature); unchanged by this phase |
| T-01-07 | Denial of Service | Firebase init ordering | medium | mitigate | Block 3 (Firebase init) placed before Block 4 (Utilities) in script order; verify checks block count + symbol presence before commit |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase — zero new dependencies (REF-05) |
</threat_model>

<verification>
- After each task: reload `index.html` via `python -m http.server` (or equivalent) and confirm the app renders identically with no console errors (locked decision D-03).
- Spot check after each extraction: request list populates from Firestore, manager passcode gate (Enter key), wrong-passcode toast, icon rendering.
- Final: `node` verify scripts above pass (block count + symbol presence).
</verification>

<success_criteria>
- index.html contains exactly 5 text/babel blocks in dependency order (Constants → Config → Firebase Init → Utilities → original inline).
- app/auth/db init exist in Block 3; isOverdue/getDaysInfo/getRequestDuration/getEquipmentLabel/getEquipmentIcon exist in Block 4; all absent from the original inline block.
- Zero user-visible change: app renders identically, no console errors, Firestore data loads, passcode gate works.
- Two commits created: `refactor(01-02): extract firebase init block`, `refactor(01-02): extract utilities block`.
- REF-01, REF-02, REF-03, REF-04, REF-05 all advanced by this plan (decomposition continued, data + utilities layers extracted, behaviors preserved, single-file deployment intact).
</success_criteria>

<output>
Create `.planning/phases/01-multi-block-decomposition/01-02-SUMMARY.md` when done
</output>